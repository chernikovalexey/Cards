import "dart:js";
import "dart:html";

class FeatureManager {
    FeatureManager() {
       // Uri url = Uri.parse(window.location.href);
        //(context['Features'] as JsObject).callMethod("start");
    }
}
